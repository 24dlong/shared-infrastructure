name: Debug Token

on:
  push:
    branches:
      - fix-ci

permissions:
  contents: read
  actions: read
  pull-requests: read

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect GITHUB_TOKEN access
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "GITHUB_REPOSITORY=${GITHUB_REPOSITORY}"
          echo "GITHUB_SHA=${GITHUB_SHA}"
          echo "--- gh api user (token identity) ---"
          gh api /rate_limit --jq '.resources.core' || true
          echo "--- raw curl with -i to see status/body ---"
          curl -s -i \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H "Accept: application/vnd.github+json" \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/pulls"
          echo ""
          echo "--- gh api (same call apply action makes) ---"
          gh api "repos/${GITHUB_REPOSITORY}/commits/${GITHUB_SHA}/pulls" --jq '.[0]' || true
